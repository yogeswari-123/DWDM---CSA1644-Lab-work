# Sample data for pie chart
sizes <- c(80, 100, 120)  # Sizes of the slices
labels <- c("vyshu", "laswi", "anju")  # Labels for the slices

# Create pie chart
pie(sizes, labels = labels, main = "Pie Chart Example")
